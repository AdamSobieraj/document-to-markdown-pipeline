# ==============================================================================
# KONFIGURACJA GŁÓWNA APLIKACJI
# ==============================================================================

# ==============================================================================
# 1 ŹRÓDŁA DANYCH (S3 / LOCAL)
# ==============================================================================

# Core Arguments
source-type: s3
dest-type: s3

#S3 Arguments
bucket: mojsuperbucket
prefix: "General/"
out-bucket: mojsuperbucket

# Local Arguments
dir: "/home/blackmain/PycharmProjects/document-to-markdown-pipeline/data_preparation/inputs/business"
out-dir: "/home/blackmain/PycharmProjects/document-to-markdown-pipeline/data_preparation/inputs/business"

# Logging Argument
debug: false

chunking:
  # ALLOWED_EXTENSIONS
  allowed_extensions:
    - ".md"
    - ".txt"
    - ".xml"
    - ".xsd"
    - ".pdf"
    - ".docx"
    - ".xlsx"
    - ".json"