# ==============================================================================
# KONFIGURACJA GŁÓWNA APLIKACJI
# ==============================================================================

# ==============================================================================
# 1 ŹRÓDŁA DANYCH (S3 / LOCAL)
# ==============================================================================

# Core Arguments
source-type: s3
dest-type: s3

#S3 Arguments
bucket: mojsuperbucket
prefix: "General/"
out-bucket: mojsuperbucket

# Local Arguments
dir: 'C:\Users\Tor.com\Desktop\PROJEKTY\document-to-markdown-pipeline\data_preparation\inputs\business'
out-dir: 'C:\Users\Tor.com\Desktop\PROJEKTY\document-to-markdown-pipeline\data_preparation\inputs\business'

# Logging Argument
debug: false

chunking:
  # ALLOWED_EXTENSIONS
  allowed_extensions:
    - ".md"
    - ".txt"
    - ".xml"
    - ".xsd"
    - ".pdf"
    - ".docx"
    - ".xlsx"
    - ".json"

  # Maximum file size in megabytes
  max_file_size_mb: 100